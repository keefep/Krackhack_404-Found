{
  "name": "iit-mandi-marketplace",
  "version": "1.0.0",
  "scripts": {
    "start:backend": "cd backend-node && npm install && npm run dev",
    "start:frontend": "cd frontend/IITMandiMarketplace && npm install && npm start",
    "dev": "concurrently \"npm run start:backend\" \"npm run start:frontend\"",
    "install:all": "npm install && cd backend-node && npm install && cd ../frontend/IITMandiMarketplace && npm install"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
